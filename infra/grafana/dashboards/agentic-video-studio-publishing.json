{
  "annotations": {"list": []},
  "editable": false,
  "graphTooltip": 1,
  "panels": [
    {
      "datasource": {"type": "grafana-clickhouse-datasource", "uid": "avs-clickhouse"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 9, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list", "placement": "bottom", "showLegend": true}},
      "targets": [{"editorType": "sql", "format": 0, "rawSql": "SELECT toStartOfMinute(occurred_at) AS time, event_type, count() AS publications FROM events_v1 WHERE $__timeFilter(occurred_at) AND event_type LIKE 'publication.%' GROUP BY time, event_type ORDER BY time", "refId": "A"}],
      "title": "Publication states",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "grafana-clickhouse-datasource", "uid": "avs-clickhouse"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 9, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "options": {"showHeader": true},
      "targets": [{"editorType": "sql", "format": 1, "rawSql": "SELECT occurred_at, event_type, resource_id, project_id, correlation_id FROM events_v1 WHERE $__timeFilter(occurred_at) AND event_type LIKE 'publication.%' ORDER BY occurred_at DESC LIMIT 100", "refId": "A"}],
      "title": "Provider activity",
      "type": "table"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 41,
  "tags": ["agentic-video-studio", "publishing"],
  "time": {"from": "now-24h", "to": "now"},
  "timezone": "browser",
  "title": "Agentic Video Studio · Publishing",
  "uid": "avs-publishing",
  "version": 1
}
