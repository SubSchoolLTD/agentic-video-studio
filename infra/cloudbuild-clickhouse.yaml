steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - infra/Dockerfile.clickhouse
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
